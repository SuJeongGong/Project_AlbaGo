<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.ex.dao.CommunityDAO">
	
	<select id ="selectCommunityList" parameterType="Community">
		select * from community
	</select>
	
	<insert id = "insetContent">
		inset into community value (null,#{individual_id},#{individual_id},#{individual_id},#{individual_id},);
	</insert>
	