<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.ex.dao.EnterpriseDAO">

	<select id="selectNow" resultType="Product" parameterType="hashmap">
		select * from enterprise where enterprise_id=#{param1} and price=#{param2};
	</select>

	<select id="selectEnterprise" parameterType="String" resultType="Enterprise">
		select *
		from enterprise 
		<where>
			enterprise_id = #{id}
		</where>
	</select>
	
	<select id="selectScrap" parameterType="String" resultType="ArrayList">
		select resume.title AS title, scrap_enterprise.date AS DATE 
		from scrap_enterprise, resume
		where scrap_enterprise.board_resume_id = resume.resume_id
		group by enterprise_id
		HAVING enterprise_id = #{id}
	</select>
	
	<select id="selectVolunteer" parameterType="String" resultType="ArrayList">
		select *
		from volunteer 
		group by #{id}
	</select>
	
	<select id="selectRecruit" parameterType="String" resultType="ArrayList">
		select title,date
		from recruit 
		group by #{id}
	</select>
</mapper>